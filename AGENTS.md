# cpp_nuget_pack

## Overview

「C++ NuGet 打包工具」：Flutter **Windows 桌面应用**（`.metadata` → `project_type: app`），目标是把 C++ 头文件/源码/库可视化成 NuGet 包。注意：

- **不是** Flutter 插件、**不是** C++ 库。名称里的 NuGet 只是应用的功能主题——构建管线与 CI 至今没有任何 NuGet 工具链集成（无 .nuspec/.targets/nuget.exe/dotnet pack）。
- 当前进度：「添加文件夹」全流程可用（目录选择 → 「添加包」表单 → 保存 YAML → 侧边栏列表更新），且「包信息」页支持编辑并保存回 YAML（版本/作者/许可证/描述可改，包 ID 不可改），工具栏「删除文件夹」可删除选中包（确认对话框仅删配置，被依赖时列出依赖方），「重新映射」可对选中包重新扫描源目录并更新包结构（进度/结果对话框）；「文件管理」目录树支持双击打开文件、lib/dll/pdb/exe 显示 Release/Debug 构建标签；包列表与「文件管理」目录树均为真实数据（配置持久化在 `config/`，见 Architecture）；「依赖管理」支持从现有包选择依赖并自定义 NuGet 版本范围（增删改自动保存、选中包预填 `[版本,)`、失效依赖显示「缺失」）；「编译设置」支持宏定义、编译前/后命令（命令对话框可从包内选择脚本并插入 MSBuild 常用宏）、附加库目录与附加库（条目均带 ALL/Release/Debug 构建标签、增删改自动保存）；「打包设置」支持选择打包格式（NuGet / CMake）、预览打包后的文件与内容（左侧文件树 + 右侧文本预览；打包包含源目录下全部文件，头文件按源目录名命名词空间（源目录已自带同名目录时不再叠加））；工具栏「打包文件夹」按「打包设置」所选格式导出（NuGet .nupkg / CMake 配置包 .zip；缺失依赖时弹窗警告、可继续；进度/完成/失败对话框，输出至设置的全局输出目录，完成后可在资源管理器中定位；包图标自动转换嵌入——检测图标转 PNG 等比缩放至 ≤128，缺失用默认纸箱图标；NuGet 格式下，源目录根部命中许可证（LICENSE/LICENCE/COPYING 等）时 `.targets` 追加 `DeployPkgLicense_<清洗包ID>_<hash8>` 目标，消费方 Build 后将许可证硬链接至 `$(OutDir)\licenses\<包ID>_license.txt`）；「历史记录」按钮弹出选中包的时间线历史（创建/版本变更/重新映射/打包导出四类事件，支持删除条目，上限 100 条）；「依赖关系图」按钮弹出全部包的依赖关系图（可拖拽平移与滚轮缩放、缺失依赖红色标注、当前选中包高亮），工具栏 6 个按钮至此全部实现；footer「设置」与「关于」均为真实页面（设置页：打包输出目录 + 编译器优先级（ICX/clang-cl/MSVC 列表，检测本机编译器与版本、可上移/下移与重新检测，保存至 `config.yaml` 的 `compilerPriority`，检测结果缓存 `detectedCompilers`——有缓存免重检、构建统一复用）+ SKILL.md 生成（内置模板写盘）+ 主题模式 + 主题配色，即时生效并持久化；关于页：应用名/版本号/描述与项目主页链接，版本号来自 `lib/app_info.dart`，与 pubspec 的同步由测试保障）。上述功能全部完成；节点编辑器 M2（编辑器 UI）随后完成：编译设置页「节点脚本」分区为入口，全屏编辑器含脚本项目 CRUD/排序、节点库、画布拖动与连线、检查器参数、PowerShell 预览与诊断联动、串行防抖保存队列（四态 + 失败重试 + 返回确认）；M3 打包接线随后完成：节点脚本编译为 `.ps1`（BOM）进入 nupkg（`build/native/files/scripts/<id>.ps1`），`.targets` 追加 `CnpScripts_<清洗包ID>_<hash8>_<Pre|Post>` 目标自动执行（`EnvironmentVariables` 传 `CNP_*`），导出前校验编译错误与包内路径查重（可继续/取消），CMake 格式不接脚本；M4.2（节点家族扩展）随后完成：注册表扩充至 10 类 42 类型（新增 math 5 / `string.upperCase` / `logic.compareNumber` / file 3 / crypto 6），4 个新参数类型（mathOperator/bitwiseOperator/numberOperator/hashAlgorithm）贯通「枚举 → 校验白名单 → 检查器 ComboBox → 值条映射」；生成器引入 prelude 前置片段机制（按需注册、固定序去重；5 个 PS 辅助函数 + CRC32 Add-Type 块），AES 与代码签名节点受节点级「二选一」校验（口令或口令环境变量名、PFX 路径或证书指纹）；M4.3（变量系统）随后完成：注册表扩充至 11 类 46 类型（新增 variable 4：写入/读取数值变量、写入/读取文本变量），变量名正则（`^[A-Za-z_][A-Za-z0-9_]*$`）与「同名同类型」校验生效（内联红字与环境变量同款），生成器发射期收集变量名、组装前注入 `vars` 初始化块（按名称字典序，数值 `0`/文本 `''`），「变量 + while + branch + 算术/比较」图灵完备可达；helper 真实执行冒烟（`ConvertFrom-CnpHex` 往返 + crc32 已知向量）入 CI；M4.4（系统/网络/脚本 + 工具打包）随后完成：注册表扩充至 12 类 51 类型（新增 system 3：查找工具/下载文件/上传文件、context 包内脚本文件、process 运行包内脚本），新增 scriptFilePath/textLines 参数类型与 system 分类；生成脚本骨架恒含 PSModulePath 入口归一（`$env:PSModulePath = "$PSHOME\Modules"`），变量名校验与初始化按小写键归并（书写形式保留首见），`math.numberToString` 修正为 `[Convert]::ToString`；导出校验对话框泛化（标题「导出校验」，NuGet 汇总脚本编译问题 + 包内路径查重 + 包内 `.exe` 警告、CMake 仅 `.exe` 警告，均可继续/取消）；语法夹具覆盖七张代表图，生成脚本真实执行冒烟（while 累加 + `crypto.fileHash` sha256 加固行为锁）入 CI。「构建」功能随后完成：库源目录根部存在 `build.py`（首行 `# <git仓库地址>`）时，【文件管理】页显示「构建」按钮——自动把源码拉取到工具根 `cache/build/<清洗包ID>/`，以 `SRC_PATH`/`BUILD_OUT` 环境变量执行构建脚本（`python`，回退 `py -3`），成功后自动重新映射；`build.py` 排除出打包产物；构建前自动准备构建环境——检测本机编译器（ICX > clang-cl > MSVC，优先级可在设置页调整）、捕获编译器环境（vcvars/setvars）、按需下载 CMake/Ninja 等工具到 `tools/` 并注入构建子进程（仅影响该次运行的命令行环境）。构建管线 v2/v3 随后完成：`build.py` 头部契约扩展（`# tool` 环境工具自动下载、`# option`/`# checkbox`/`# multiselect` 构建选项 UI（下拉/复选/多选）、`# runtime:` 运行库家族（MD/MT 由 App 解析经 `CNP_RUNTIME_LIBRARY` 下发）、`# source: none` 预构建配方、`# depends` 依赖自动注册；设置页「生成 SKILL.md」按钮供 AI 插件分发）；产物经分类辅助模块按 `release/`、`debug/` 分层归档（自动注入 AVX2、Release 最高优化与 LTO、多线程参数）；工具供给覆盖 Python/CMake/Ninja/LLVM（解析最新版本、编译器全缺时 clang 兜底、受控 TMP/TEMP 注入）；构建输出实时显示于进度对话框右侧面板（ERROR/WARNING/INFO 高亮）；根级 `pre.bat`/`post.bat` 与 `# depends` 自动注册为系统条目（界面禁改删）；含仓库的包显示 git/更新徽标与当前/最新版本号；八个真实库配方（zlib/xsimd/proxy/gtest/sqlite3/mimalloc/OpenVINO/OpenSSL）真机验收全绿。
- 仅支持 Windows（无 android/ios/linux/macos/web 平台目录）。

## Commands

```text
flutter pub get
flutter analyze                    # 静态检查（CI 门禁，须零问题）；analysis_options.yaml 排除 build/**、windows/**、android/**
flutter test                       # 运行测试（test/：冒烟 + 模型 + 扫描 + 配置存储 + 对话框/接线 + 页面 + 列表/工具（含 file_opener 路径规范化）+ 悬浮提示 + 图标映射 + 构建标签 + 依赖管理/版本范围 + 设置页/主题（含检测缓存显示/写回） + 编译设置 + 打包计划/预览/导出/包图标 + CMake 打包 + 缺失依赖警告 + 历史记录 + 关于页/应用信息 + 脚本编辑器（节点注册表/宏白名单/图校验/PS 生成器/语法解析（七图）/prelude 辅助函数（含 crc32 块）/prelude 真实执行冒烟（helper + 生成脚本：while 累加与 `crypto.fileHash` sha256 加固行为锁）/变量系统（发射/校验/大小写归并初始化）/值条显示 + M2 编辑器：状态控制器/保存队列/画布/节点库/检查器/输出面板/项目对话框/编辑器页）+ M3 打包接线：SHA-1/脚本条目与 Target 片段/nupkg 含 .ps1/导出校验（脚本问题/路径查重/exe 警告）/.targets XML 良构性与 CMake 坏脚本护栏 + 编译前/后命令自定义 `Exec` 目标发射（命名/条件/转义/多命令顺序）+ 许可证识别与 `.targets` 部署目标发射 + 构建（build.py 解析/`build_runner`/构建对话框/工具栏接线/打包排除/构建环境：编译器检测与优先级、检测缓存复用（有效直用/失效重检回调/受控 TMP 注入）、vcvars 捕获、CMake-Ninja 供给与环境装配、环境注入（含 `CNP_RUNTIME_LIBRARY` 运行库下发）、真实冒烟（gated）、契约（头部 `# tool`/`# option`/`# checkbox`/`# multiselect`/`# runtime` 解析与选项取值）/选项 UI（三型控件渲染与保存、运行库选择器与帮助 Tooltip、折叠与纵向滚动、保存中禁用、非法值回退）/`buildOptions` 序列化/分类辅助模块（真机分类冒烟；运行库家族 md/mt/非法回退；`stage_binaries` 段重置/默认兼容/双跑不累积；`classify_tree` 的 `_debug` 文件名后缀与缺省参数一致性）/SKILL.md 生成（asset 载入与写出）/构建 v2 延伸（`# source: none`/`# depends` 契约、分类分层与优化参数、Python 供给、TMP/TEMP 注入、ICX 多根检测、clang 兜底供给、最新版解析、系统条目、构建输出面板、git 版本徽标、库配方真机验收（zlib/xsimd/proxy/gtest/sqlite3/mimalloc/OpenVINO/OpenSSL，gated、zlib 纯度断言（release 精确集合、无 `zlibd*`/`zlibsd*`/`_build-*`/`.pdb`，debug 无 `_build-*`）、mimalloc post.bat 编码（ANSI/ASCII、cmd 无参数调用静默）、流式输出（git/python 逐行转发与 `\r` 切分、`-u` 参数、`runPackBuildStreaming` 生产接线、`PYTHONIOENCODING=utf-8` 注入与无效字节容错/真实进程中文输出）、下载进度（按块回调字节与速度、透传、自定义 fetch 不回调）、sourceNone 阶段序列与分类标记、失败保留输出）、构建前清理与预构建缓存复用加固（`build_cleanup` 白名单/根级 `.git` 保留/递归删除/占用报错与 `BuildCleanupException` 包装、git 硬重置序列与失败语义（tag 检出与回退链、绝不盲用 FETCH_HEAD）、source:none 缓存保留、相对 cacheRoot 解析、OpenVINO `.complete` 标记绑定归档驱动））））
flutter build windows --release    # 产物：build/windows/x64/runner/Release/
flutter run -d windows             # 本地运行
```

- `pub get → analyze → test → build windows --release` 序列来自 `.github/workflows/ci.yml`（命令事实源）。
- Windows 构建走 CMake + MSVC（C++17、`/W4 /WX` 警告即错误，见 `windows/CMakeLists.txt`），需要 Visual Studio C++ 工具链；本机缺工具链时先 `flutter doctor` 确认，构建交给 CI。
- SDK 约束：Dart `^3.13.2`、Flutter `>=3.44.0`（本地环境 Flutter 3.47.2 stable）。

## 发布流程（勿误触发）

- **CI 在 push `master` / `dev` 时触发**：`analyze → test → build windows --release`（Flutter SDK 与 pub 依赖由 `subosito/flutter-action` 缓存）。**仅 `master` 发布**：打包 `dist/cpp_nuget_pack-<版本>-win-x86_64.zip` → 创建 GitHub Release（tag `v<版本>`，如 `v26.1`）；`dev` 渠道只验证（测试+构建）不发布。无 PR 检查。
- 开发在 `dev` 分支进行（日常推送不触发发布）；发布时把 `dev` 合并/推送到 `master` 并升级版本号。
- 版本号采用「年份.年内发布数量」方案（如 `26.1` = 2026 年第 1 次发布；年内递增 `26.2`、`26.3`…；次年从 `27.1` 重计；更早的 `1.0.x` 为历史版本）。唯一来源是 `pubspec.yaml` 的 `version:`（存三段格式 `26.1.0+1`，第三段固定 0；CI 去掉 `+build` 并去掉尾部 `.0` → tag `v26.1`，解析失败回退为时间戳）。同一版本经 CMake `FLUTTER_VERSION*` 宏注入 exe 文件版本（`windows/runner/Runner.rc`）。应用内「关于」页显示的版本来自 `lib/app_info.dart` 的 `appVersion`（去掉 `+build` 与末尾 `.0`，即 `26.1`）——升级版本号时须同步该常量（`test/app_info_test.dart` 强制校验一致性）。
- 所以「改 `pubspec.yaml` 版本号 + push `master` = 发 Release」——没有发布意图时不要动版本号。

## Architecture

- 入口链：`lib/main.dart` → `MainLayout` → `lib/controls/pack_list.dart` / `pack_manage.dart` → `lib/pages/*`。
- 「添加文件夹」流程：`MainLayout.pickDirectory`（默认 `file_selector` 的 `getDirectoryPath()`，系统原生目录对话框，取消返回 null）→ `lib/controls/add_directory_dialog.dart`「添加包」对话框（分阶段：扫描进行中仅显示进度，完成后才显示表单——路径 + 扫描统计 + 包 ID/版本/作者（必填）/许可证（下拉可空，8 项 SPDX）/描述 + 图标自动识别扫描结果首个图片文件（png/jpg/jpeg/svg/ico/webp）；「确定」返回 `PackModel`（含 `files`/`license`/`iconPath`/`sourcePath`）→ `PackStore.savePack` 写入 YAML 并更新侧边栏列表（同 ID 覆盖、自动选中）。扫描经 `MainLayout.scanFiles`，默认 `FileScan.scan`。`pickDirectory`/`scanFiles`/`store` 均可注入以配合测试。
- 配置持久化在 `lib/config/pack_store.dart`（唯一 YAML 读写点）：根目录为工作目录相对 `config/`（便携）；启动时 `ensureConfigExist()`（`config/`、`config/packs/`、`config.yaml`）+ `loadPacks()` 逐个解析 `packs/*.yaml`，损坏或缺必填字段的文件跳过、启动后以右上角悬浮提示（error、5 秒、多条合并）列出警告（不静默）；`savePack()` 文件名 = 包 ID 清洗（`sanitizeFileName`，非法字符 → `_`，同 ID 覆盖）；`deletePack()` 按同规则删除对应 YAML（幂等，文件不存在视为成功）。YAML 由 `yaml`/`yaml_edit` 生成：null 字段省略、多行描述用 `|-` 块标量；包配置含 `files`/`dependencies`/`commands`/`macros`/`libDirectories`/`libraries`/`history`/`scripts` 八类列表（缺失视为空；`scripts` 级警告经 `PackLoadError` 并入启动提示）。另有 `loadSettings()`/`saveSettings()` 读写 `config.yaml` 中的全局设置（打包输出目录/主题模式/深色配色/强调色；缺文件或损坏回退默认值）。
- 侧边栏列表由 `PackList.buildCards(packs, onSave:)` 从真实包列表动态构建，列表项图标取自包 `iconPath`（与 `sourcePath` 拼接；svg 走 `SvgPicture.file`、位图走 `Image.file`，缺失/加载失败时兜底默认图标）；选中包后右侧为「包信息」与「文件管理」（`pack_files.dart` 目录结构树：fluent_ui `TreeView`，目录/文件均显示大小、目录为后代聚合且名称后追加 `(N 个文件)` 后代文件计数，默认全部折叠；树图标为 catppuccin/vscode-icons 子集双套——latte/mocha 随主题亮度、具名目录与展开态 `_open`，解析见 `lib/util/catppuccin_icons.dart`；双击文件行用系统默认程序打开（`lib/util/file_opener.dart`），lib/dll/pdb/exe 文件按相对路径推断在名称后显示 Release（绿）/Debug（橙）标签、推断不出不显示（`lib/util/build_config.dart`，另提供 `allBuildLabel`/`buildModelLabel()`/`buildModelColor()`/`scriptTriggerLabel()`/`scriptTriggerColor()` 共享映射，供编译设置与节点编辑器复用））；无包时显示引导文案。`lib/util/format.dart` 提供共享的 `formatBytes()`/`baseName()`/`joinPath()`/`formatError()`（错误文案，ArgumentError 去前缀）；`lib/util/file_image.dart` 提供共享的 `buildFileImage()` 与图标识别 `findIconFile()`（对话框预览与侧边栏共用）；`lib/util/licenses.dart` 提供共享 SPDX 许可证列表与下拉框等高常量 `comboBoxDensity`（许可证、依赖包名下拉框与相邻输入框等高）；`lib/util/catppuccin_icons.dart` 提供目录树图标资产路径解析 `iconAssetFor()`（文件名 > 前缀 > 扩展名 > 兜底；latte/mocha 随 `Brightness`）；`lib/util/version_range.dart` 提供 NuGet 版本范围校验 `versionRangeError()`/`isValidVersionRange()`（`[ ]` 含端点、`( )` 不含，裸版本 = 最低版本、含预发布排序比较；拒绝浮版本与零宽度区间）。
- 「包信息」编辑流（`lib/pages/pack_info.dart`）：默认只读，点「编辑」后版本/作者/描述/许可证可改（包 ID 永远只读——它同时是 YAML 文件名）；「保存」需必填有效且有改动，「取消」还原原值；保存挂起期间切换包则丢弃回写；成功经 `onSave` 回调链写盘（`PackList → PackManage → PackInfo → MainLayout._savePack`）并弹出右上角悬浮提示「已保存」，失败弹「保存失败」对话框且停留编辑态。`PackManage` 持稳定 `Tab` 实例 + `ValueNotifier<PackModel>` 推送包更新（fluent_ui `TabView` 每次 build 重建 `Tab` 会销毁页面 state）。
- 「删除包」流程：工具栏「删除文件夹」按钮（未选中包或选中 footer「设置/关于」时禁用）→ `lib/controls/delete_pack_dialog.dart` 确认对话框（「删除」红底白字居左、「取消」居右；若被其他包依赖，以表格（包名/版本范围两列）列出依赖方并提示将显示「缺失」——`dependents` 参数为 `List<PackDependent>`，即 `({String name, String version})` 记录，由 `MainLayout` 收集依赖方的名称与其依赖所要求的版本范围）→ `PackStore.deletePack()` 仅删除配置文件（源目录不动）；成功后从列表移除并调整选择（保持索引位、越界回退末项、空列表置空），悬浮提示「已删除」；失败提示「删除失败」（error、5 秒）。
- 「重新映射」流程：工具栏「重新映射」按钮（未选中包或选中 footer「设置/关于」时禁用）→ 选中包重新扫描 `sourcePath`（缺失时 error 悬浮提示、不弹对话框）→ `lib/controls/remap_pack_dialog.dart`（扫描中 → 更新中 → 完成/失败：显示文件数量、总大小、新增/移除 N，与旧快照按路径大小写不敏感对比）→ 扫描成功后自动 `savePack` 写盘并刷新列表/详情，图标按新结果重识别；扫描完成前关闭对话框即取消（不写盘）。
- 「依赖管理」流程：`PackManage` 第 3 Tab（`lib/pages/pack_dependencies.dart`）列出依赖（表格三列：包名/版本范围/操作，表头下与行间分隔线）；「添加依赖」经 `lib/controls/dependency_dialog.dart` 从现有包下拉选择（排除自身与已添加、无候选时提示；选中后自动预填 `[该包版本,)`），版本范围实时校验（非法提示并禁用「确定」）；指向不存在包的依赖显示红色「缺失」标签；增删改均自动保存（构造全字段拷贝的 `PackModel` → `onSave` → 成功悬浮提示「已添加/已保存/已删除」）；依赖持久化为 YAML `dependencies: [{name, version}]`。
- 「编译设置」流程：`PackManage` 第 4 Tab（`lib/pages/pack_compile_settings.dart`）分 5 区：宏定义（单栏「名称=值」文本）、编译前命令、编译后命令、附加库目录（文本 + 「浏览…」调 `getDirectoryPath`）、附加库；各分区渲染为表格（首列标签按分区取宏定义/命令/目录路径/库名称，加构建配置标签列与操作列；表头下与行间分隔线），条目行 = 内容 + 构建标签（`buildModelLabel()`，ALL 蓝/Release 绿/Debug 橙）+ 编辑/删除；条目录入经 `lib/controls/compile_entry_dialog.dart`（单文本框 + 构建配置下拉，编辑预填；编译前/后命令对话框另提供「从包中选择」脚本下拉——exe/bat/cmd 插入 `"$(MSBuildThisFileDirectory)files\...\"` 引用路径、ps1/py 自动加解释器前缀——与「插入宏」下拉（16 项常用 `$(...)`/`%(...)` 宏，光标处插入））；增删改自动保存（全字段拷贝 `PackModel` → `onSave` → 悬浮提示「已添加/已保存/已删除」）；持久化为 YAML `macros: [{value, buildModel}]`、`commands: [{command, type, buildModel}]`、`libDirectories: [{path, buildModel}]`、`libraries: [{name, buildModel}]`。
- 「打包设置」流程：`PackManage` 第 5 Tab（`lib/pages/pack_packaging.dart`）选择打包格式（下拉来自 `PackageBuilderRegistry`：NuGet、CMake）并「预览打包内容」→ 所选格式的 `buildPlan()` 生成 `PackagePlan` → `lib/controls/pack_preview_dialog.dart` 预览对话框（左侧包内文件树默认折叠 + 右侧等宽字体文本预览：生成文件直接显示、源文本文件读盘、二进制提示不可预览，读取函数可注入）；缺 `sourcePath` 时悬浮提示无法预览。选中格式为会话状态（`MainLayout._packagingBuilder`，经 `PackList`/`PackManage` 下传，默认 NuGet），工具栏「打包文件夹」按所选格式分派导出。
- 打包抽象层在 `lib/packaging/`：`package_builder.dart` 定义 `PackageBuilder` 接口（`id`/`displayName`/`buildPlan()`）与 `PackageBuilderRegistry` 注册表（可扩展新格式）；`package_plan.dart` 定义 `PackagePlan`/`PackageEntry`（sealed 来源：源文件/生成文本；包内路径 `/` 分隔、确定性排序）与 `buildNativeRelativePath()`/`includeNamespaceOf()`/`includePackageRelativePath()`（包内路径公共助手：剥离 `build/native/` 前缀供 `.targets` 生成与检查器建议值共用；头文件命名空间与首段同名去重复层供 NuGet/CMake 构建器共用）；`nuget_builder.dart` 实现 NuGet 布局——头文件/模块 → `build/native/include/<源目录名>/`（剥离开头 `include/` 后再套一层源目录名命名词空间，源目录已自带同名首段（大小写不敏感）时不再叠加，消费者可写 `#include <源目录名/foo.h>` 防头文件冲突，源目录缺失时回退包名）、lib/dll/pdb → `build/native/lib/`（剥离开头 `lib|bin`）、其余文件 → `build/native/files/`（保留原相对路径、全部入包）；另生成包根 `<包ID>.nuspec`（license 用 SPDX 表达式、依赖组 `native0.0`、版本去 `+build`、XML 实体转义、恒定声明 `<icon>images\icon.png</icon>`）与 `build/native/<包ID>.targets`（include 行 + 宏/附加库目录/附加库的 ALL/Release/Debug 条件分组；.lib 按路径推断 D/R 自动同时补库目录与库名；包内有 .asm 时条件导入 VS `masm.props/masm.targets` 并生成 `MASM` 项；.rc 生成 `ResourceCompile` 项；dll/pdb 经 `DeployPkgRuntimeBinaries` 目标（`AfterTargets="Build"`）以 `Copy UseHardlinksIfPossible` 硬链接到 `$(OutDir)`（失败回退拷贝，`FileWrites` 供清理）；`license_file.dart` 识别源目录根部许可证（`findPrimaryLicensePath()`：仅根目录、LICENSE/LICENCE/COPYING/UNLICENSE/NOTICE 及 `-`/`.` 变体、大小写不敏感、核心名优先级 + 同核心名字典序），命中时追加 `DeployPkgLicense_<清洗包ID>_<hash8>` 目标（`AfterTargets="Build"`，`Copy UseHardlinksIfPossible` 硬链接到 `$(OutDir)licenses\<包ID>_license.txt`，失败回退拷贝，`FileWrites` 供清理；无许可证零变化）；编译前/后命令发射为自定义 `Exec` 目标 `CnpPreBuild_<清洗包ID>_<hash8>`（`BeforeTargets="ClCompile"`）/`CnpPostBuild_<清洗包ID>_<hash8>`（`AfterTargets="Build"`，每命令一条 `Exec`，`WorkingDirectory="$(ProjectDir)"`、`IgnoreStandardErrorWarningFormat`；Release/Debug 组以目标名 `_Release`/`_Debug` 后缀 + 目标级条件区分）——VS v180 的 C++ 事件目标读取项元数据 `%(PreBuildEvent.Command)`/`%(PostBuildEvent.Command)`，晚导入的 NuGet `.targets` 属性不被消费、命令静默不执行（实测），故不写事件属性）；`cmake_builder.dart` 实现 CMake find_package 配置包——头文件/模块 → `include/<源目录名>/`（源目录已自带同名首段时不再叠加）、lib/dll/pdb → `lib/`（剥离开头 `lib|bin`）、其余 → `files/`；另生成 `lib/cmake/<包名>/` 下 `<包名>Config.cmake`（自带 `check_required_components` 宏）、`<包名>Targets.cmake`（4 层 `_IMPORT_PREFIX` 前缀上溯、`<包名>::<包名>` INTERFACE IMPORTED 目标、`.lib` 按路径推断 D/R 分组以 `$<CONFIG:Debug>` 生成器表达式条件链接）与 `<包名>ConfigVersion.cmake`（版本为数字点分时生成、SameMajorVersion 语义）；`cmake_exporter.dart` 的 `exportCmakePackage()` 导出 `<包ID>-<版本>-cmake.zip`（plan 原样打包，无 OPC/图标）。
- 「打包文件夹」流程：工具栏「打包文件夹」按钮（未选中包或选中 footer「设置/关于」时禁用）→ 校验全局输出目录（未设置时 error 悬浮提示「请先在设置页配置打包输出目录」）与 `sourcePath` → 缺失依赖警告（`lib/controls/missing_dependencies_dialog.dart`：依赖指向的包不在本地列表时弹表格化警告、可继续或取消）→ 按所选格式分派导出（NuGet → `exportNuGetPackage`；CMake → `exportCmakePackage`）→ `lib/controls/pack_export_dialog.dart` 进度对话框（进行中 ProgressRing、关闭禁用 → 完成：输出路径/文件数量/包大小 + 「打开所在目录」/失败：去前缀错误文案）→ `lib/packaging/nupkg_exporter.dart` 的 `exportNuGetPackage()` 把 `PackagePlan` 组装为真实 .nupkg（`archive` 包编码；写 OPC 三件套 `[Content_Types].xml`/`_rels/.rels`/`nuget.psmdcp`；恒定嵌入 `images/icon.png`——经 `lib/packaging/package_icon.dart` 把 `iconPath`（SVG/位图）等比缩放至最长边 ≤128 统一转 PNG，缺失/损坏回退 `assets/icons/cardboard_box.svg`，`iconResolver` 可注入以配合测试；文件名 `<包ID>.<去 +build 版本>.nupkg`；先写 `.tmp` 再改名；输出目录自动创建）。`MainLayout.exportPackage` 可注入以配合测试。
- 「历史记录」流程：工具栏「历史记录」按钮（未选中包或选中 footer「设置/关于」时禁用）→ `lib/controls/pack_history_dialog.dart` 时间线对话框（最新在上，类型着色圆点/标签：创建/版本/映射/打包；删除条目无确认）；四类事件由 `MainLayout` 统一记录（`lib/models/history_model.dart` 的 `appendHistoryEntry()`，上限 100 条淘汰最旧）：创建包、版本变更（`_savePack` 对比旧包版本）、重新映射有变化（`_applyRemap` 对比路径集/总大小）、打包导出（`PackExportDialog.onExported`）；持久化为 YAML `history: [{time, type, message}]`。
- 「依赖关系图」流程：工具栏「依赖关系图」按钮（未选中包或选中 footer「设置/关于」时禁用）→ `lib/controls/dependency_graph_dialog.dart` 对话框（全图：依赖层级列布局（列距 220/行距 64）、`InteractiveViewer` 拖拽平移 + 滚轮缩放 0.5–2、节点卡片含包名/版本/红色「缺失」标签、当前选中包描边高亮；箭头由被依赖者指向依赖者；同名依赖大小写不敏感去重、自依赖跳过；「关闭」退出）。
- 「设置」页（`lib/pages/setting.dart`，footer「设置」）：打包输出目录（系统原生目录选择 + 清除）、编译器优先级（ICX/clang-cl/MSVC 列表：检测本机编译器与版本、上移/下移与重新检测，保存至 `config.yaml` 的 `compilerPriority`；检测结果缓存至 `detectedCompilers`，有缓存直接显示、无缓存自动检测并写回、「重新检测」更新写回，检测与构建统一走受控 TMP/TEMP 路径）、SKILL.md 生成（「生成 SKILL.md…」按钮经系统保存对话框写出内置模板）、主题模式（系统/深色/浅色）、深色主题配色（Frappe/Macchiato/Mocha）与强调色（14 个 Catppuccin 标准色）；改动即时生效并自动保存至 `config.yaml`（`PackTool` 有状态持有设置并重建 `FluentApp`，浮层提示「已保存」）；浅色模式固定 Latte；`buildTheme(brightness, flavor, accentName)`、`flavorByName()`、`accentColorFor()` 与两个名单常量见 `lib/util/colors.dart`。
- 「关于」页（`lib/pages/about.dart`，footer「关于」）：纸箱 Logo + 应用名 + 版本标签 + 描述 + 「项目主页」按钮（`openExternalUrl()` 经 explorer 打开默认浏览器，失败悬浮提示）+ 第三方声明指引；应用信息常量在 `lib/app_info.dart`（应用名/版本/描述/仓库地址）。
- 节点脚本引擎（M1，`lib/script_editor/`，经 M2 编辑器与 M3 打包链路接入）：`node_type.dart` 定义引脚/参数/分类/数据类型描述符（M4.1 新增 `number` 数据类型；M4.2 新增 mathOperator/bitwiseOperator/numberOperator/hashAlgorithm 参数类型；M4.4 新增 scriptFilePath/textLines 参数类型）；`node_registry.dart` 注册 12 类 51 个节点（流控 4/文件 9/进程 2/上下文 4/常量 3/字符串 7/日志 1/逻辑 3/数值 5/编码与安全 6/系统与网络 3/变量 4，引脚与参数默认值清单由测试锁定，输入引脚除 `process.run`/`process.runScript` 的参数与工作目录四个可选外全部必填）；`graph_validation.dart` 校验图结构（恰好一个入口、未知类型/悬空边/不存在引脚/必填输入未连接/种类与数据类型匹配/单连接约束/无环），产出 `ScriptDiagnostic`（error 阻断生成；warning 不阻断，如未使用数据节点、分支双出口未连接），另含节点级规则（M4.2：AES 口令二选一、签名证书来源二选一、口令环境变量名格式；M4.3：变量名正则与「同名同类型」；M4.4：findTool 工具名必填）；`variable_rules.dart` 提供变量类型集合公共常量（`variableTypeVariants` + `variableNameNodeTypes`，校验器与检查器共用防漂移）；`msbuild_macros.dart` 提供 11 项 MSBuild 宏白名单与 `macroEnvName()`（宏键 → `CNP_*` 环境变量）；`codegen/` 为生成层——`script_code_generator.dart` 定义 `ScriptCodeGenerator` 接口（`fileExtension` + `compile(project, packName:)` → `ScriptCompileResult{code, diagnostics, hasErrors}`）、`code_writer.dart` 缩进写入、`prelude.dart` 定义前置片段库（5 个 PS 辅助函数 ConvertFrom-CnpHex/Find-CnpTool/Get-CnpFileHash/Invoke-CnpAesTransform/Invoke-CnpSignFile + 动态 CRC32 块与 `vars` 变量初始化块；`preludeOrder` 固定序、按需注册幂等；骨架恒含 PSModulePath 入口归一行（相对 M4.1 基线差异仅该行））、`powershell5_generator.dart` 对全部 51 个节点发射 PowerShell 5.1（UTF-8 BOM + LF；变量节点：set → `$var_<name> = <值表达式>`、get → `$var_<name>`，发射期收集变量名（小写键归并、首见书写形式）去重、组装前按名称字典序注入 `vars` 初始化块，收集口径 = 发射路径上的变量；数字字面量 `numberLiteral()`：int 直出、double 去 `e+` 归一、负值加括号；`math.numberToString` → `([Convert]::ToString(<n>, [Globalization.CultureInfo]::InvariantCulture))`；`$ErrorActionPreference='Stop'` 后固定插入 `$env:PSModulePath = "$PSHOME\Modules"`（入口归一）再 `try {`/catch 包裹；数据节点递归内联表达式；分支/循环嵌套，循环变量 `$item_N`、进程变量 `$proc_N`/`$args_N` 全局唯一；字符串单引号转义；`context.packageFile`/`context.scriptFile`/`process.runScript.script` → `Join-Path $env:CNP_PackageRoot '<build/native 相对路径>'`（值口径：相对包根 `build/native/`，即相对 `$(MSBuildThisFileDirectory)`；不追加 `files\` 前缀）；`process.runScript` 按扩展名分派解释器（ps1 → `powershell.exe -NoProfile -NonInteractive -ExecutionPolicy Bypass -File`、py → `python`、bat/cmd/exe 直调，`-File` 之后仅脚本路径与参数）；`system.findTool` 两输出各自内联 `Find-CnpTool`（candidates 数组字面量）、`system.download`/`upload` 用 `Net.WebClient`、`crypto.fileHash` 经 `Get-CnpFileHash`（仅 crc32 注入 Add-Type 块）；生成脚本经 PowerShell 5.1 `Parser::ParseFile` 语法解析测试（七张代表图，仅解析不执行））。
- 节点编辑器 M2（编辑器 UI）：入口在「编译设置」页「节点脚本」分区（只读状态列表 + 「打开节点编辑器」按钮，`lib/pages/pack_compile_settings.dart`），打开 `lib/pages/script_editor.dart` 全屏页（`FluentPageRoute` + `Navigator.push`）。状态与图操作在 `lib/script_editor/graph_editor_controller.dart` 的 `GraphEditorController`（ChangeNotifier，图/选中/视口/诊断唯一来源；节点 id 取现有 `n<数字>` 最大值 +1；`addNode`/`removeNode`/`moveNode`（负坐标 clamp ≥0）/`updateNodeParam`（仅接受 String/bool/num/List<String>）/`connect`（仅输出→输入、占用引脚静默替换、失败返回原因文案）/选中互斥/`setViewport`/`loadProject`，图变更后即时 `GraphValidator.validate` 重算诊断）；保存队列在 `lib/script_editor/editor_save_queue.dart` 的 `EditorSaveQueue`（串行、400ms 防抖可注入、保存中追加一次 last-write-wins、四态 + 失败重试、`flush`/`waitIdle`）；值条显示纯函数在 `lib/script_editor/node_value_display.dart`（`paramValueText()`/`nodeValueSummary()`：参数值 → 显示文本、` · ` 拼接、空值跳过、空汇总 `—`）。页面装配 `lib/controls/script_editor/` 的以下组件：`editor_canvas.dart`（InteractiveViewer 0.5–2 平移缩放、场景网格与尺寸、节点拖动、引脚拖拽建连与静默替换、非法落点红环红闪 + 悬浮提示、右键菜单、Del/Esc/Ctrl+S、视口对账 `_lastSyncedViewport`）、`node_card.dart`（卡片/引脚几何与状态；M4.1 值条：参数声明非空时引脚区下方高 22 单行显示参数汇总、`scale < 0.75` 隐藏、无参数节点几何不变）、`edge_painter.dart`（贝塞尔连线/选中/预览）、`node_library_panel.dart`（分组/搜索/点击与拖拽添加）、`node_inspector.dart`（按参数类型渲染控件、项目属性与执行顺序排序、内联重命名；`scriptFilePath` 参数为源目录脚本下拉——项取 `pack.files` 脚本映射 `files/<相对路径>`，当前值缺列时首项回显）、`editor_context_menu.dart`（画布右键菜单构建：添加节点分类子菜单（`ScriptNodeCategory.values` 声明序）/重置视图/删除所选）、`output_panel.dart`（代码预览/诊断清单/150ms 开合/诊断行点击居中）、`script_project_dialogs.dart`（新建/重命名/删除对话框，id `script_<最大序号+1>`）；保存经页面 `_buildUpdatedPack()` 全字段拷贝替换当前项目后交 `onSave` 写 YAML，返回前 flush 视口与队列、失败弹确认对话框（重试/仍返回）。
- 节点编辑器 M3（打包接线）：`lib/packaging/script_packaging.dart` 的 `ScriptPackaging.build()` 把 `pack.scripts` 逐脚本经 `PowerShell5Generator` 编译（失败跳过并收集 `PackagingIssue`，**不抛异常**），产出 `.ps1` 生成条目（`build/native/files/scripts/<id>.ps1`，`<id>` 已含 `script_` 前缀；BOM 由生成器前置 `\uFEFF`）与 `.targets` 片段：每 trigger 一个 `CnpScripts_<清洗包ID>_<hash8>_<Pre|Post>` Target（pre 挂 `BeforeTargets="PreBuildEvent"`；post 挂 `AfterTargets="DeployPkgRuntimeBinaries"`，包内无 dll/pdb 时挂 `AfterTargets="Build"`；`hash8` 为 `lib/util/sha1.dart` 纯 Dart SHA-1 前 8 位，防跨包 Target 重名静默覆盖），每脚本一条 `Exec`（`powershell.exe -NoProfile -NonInteractive -ExecutionPolicy Bypass -File`、ALL/Release/Debug 条件、`IgnoreStandardErrorWarningFormat`、`EnvironmentVariables` 恒含 `CNP_PackageRoot=$(MSBuildThisFileDirectory)` + 图中用到的宏按白名单顺序 `CNP_<Key>`（`;`/`%` 转义））；`nuget_builder.dart` 的 `buildPlan` 在文件条目后接入脚本条目、`.targets` 在 `</Project>` 前追加片段（无脚本包零变化）；导出前校验在 `main.dart` `_packSelectedPack`（缺失依赖对话框后）：NuGet 汇总 `collectPackagingIssues()` 脚本编译问题 + `duplicatePackagePaths()` 大小写不敏感查重 + `collectExecutableWarnings()` 包内 `.exe` 警告（大小写不敏感），CMake 仅 `.exe` 警告；非空弹 `lib/controls/packaging_issues_dialog.dart` 的 `showPackagingIssuesDialog`（标题「导出校验」，`.exe` 警告存在时展示供应链提示；取消中止/继续导出）；CMake 不接脚本（预览/导出均不出现脚本条目）。
- 「构建」流程：库源目录根部存在 `build.py`（大小写不敏感）时，【文件管理】Tab 顶部显示「构建」按钮（`lib/pages/pack_files.dart`，`Key('buildPackButton')`；回调链 `MainLayout._buildPack → PackList.buildCards → PackManage → PackFiles`，`MainLayout.buildPack` 可注入）。`lib/controls/build_pack_dialog.dart` 阶段对话框（准备环境（显示工具下载进度/百分比/速度，Key `buildDownloadProgress`）→ 下载源码 → 执行构建 → 重新映射 → 完成/失败；`# source: none` 预构建包隐藏准备/构建文案，阶段为 正在下载（可带 `progress NN%` 百分比）→ 正在分类（`classify_tree` 开始标记触发）→ 重新映射；失败保留已流式显示的输出并补充输出尾部（去重）；输出面板恒含顶部日志过滤框（`Key('buildOutputFilter')`，大小写不敏感子串、可清除，过滤后无命中显示「无匹配行」、原始输出行列表不受影响）与共享横向滚动（`Key('buildOutputHorizontalScrollbar')` 可见可拖拽的横向 Scrollbar + 鼠标可拖拽内容，长行不换行、垂直自动滚底保留））；`lib/build/build_script.dart` 提供 `isBuildScriptPath()`/`findBuildScript()`/`parseBuildScriptRepo()`（首行 `# <仓库地址>`，`#` 后内容非空）；`lib/build/build_runner.dart` 的 `runPackBuild()`/`runPackBuildStreaming()`（后者为 `MainLayout.buildPack` 生产默认：注入 `Process.start` 流式执行器；`PackBuildRunner`/`PackProcessRunner`/`PackStreamingProcessRunner` 类型、`PackBuildStage` 阶段、`PackBuildException.outputTail`）把源码 clone / 原位硬重置到 `cache/build/<清洗包ID>/`（[cacheRoot] 以绝对路径解析保证同一工作目录缓存稳定命中；`GIT_TERMINAL_PROMPT=0` 防凭据卡死；clone 失败清理残留；已有 `.git` 时反复制拉取改为 `fetch --progress` → `reset --hard` 上游跟踪分支（`@{u}` 取不到依次回退 `origin/HEAD` 与 `git remote set-head origin --auto` 刷新后的远端默认分支，**绝不盲用 `FETCH_HEAD`**，全失败抛「拉取源码失败」并提示删除缓存目录）→ 源码版本对齐（`resolveLatestStableTag()` 经 `git ls-remote --tags --refs origin` 解析最新稳定 tag，与 UI「最新版本」同一 `latestTag()` 口径/数值比较；命中即 `git checkout --force` 该 tag（仅检出成功才记录版本号），无 tag 或检出失败保持默认分支继续）→ `clean -ffdx`（在 tag 检出**之后**执行，清含 ignored 的中间产物、保留 `.git`），任一步失败抛「拉取源码失败」；流式执行器注入时 git 与 python 均逐行转发输出（无效 UTF-8 字节按 U+FFFD 容错，不因本地化工具输出中断），`\r` 分隔的 git 进度按行切分）；源码（或 `# source: none` 预构建工作区）就绪后、执行脚本前调用 `lib/build/build_cleanup.dart` 的 `cleanupBuildOutput()` 清空包源目录中白名单外的一切（递归删除，白名单 = 根级 `build.py`（大小写不敏感）+ `icon.*` + `pre.bat`/`post.bat` + 根级 `.git`（防用户把源目录当工作仓库的灾难场景，打包扫描本就跳过隐藏目录）+ 许可证类文件（`license_file.dart` 规则的超集：额外支持 `TBB-LICENSE` 类单段前缀变体，多段前缀如 `third-party-LICENSE` 不保护；`isPreservedEntryName()`/`isLicenseLikeFileName()`）；删除失败抛 `BuildCleanupException`（中文文案、列失败路径），构建入口包装为 `PackBuildException`；git 失败时不触碰源目录），再以工作目录=包源目录运行 `python -u build.py`（`ProcessException` 时回退 `py -3 -u`），环境变量 `SRC_PATH`=下载目录、`BUILD_OUT`=包源目录、`PYTHONIOENCODING=utf-8`（中文 Windows 下 python 管道默认 GBK，固定为 UTF-8 与流式解码口径一致，注入的同名键不覆盖）；非零退出抛 `PackBuildException`（输出尾部约 20 行）。成功后自动重新映射（`scanFiles` → `copyPackWithFiles()` → `_applyRemap` 保存/刷新/记 filesChanged 历史）。根级 `build.py` 从 NuGet/CMake 两种格式的产物中排除（子目录 build.py 保留）；重映射的对比与更新包构造提取为共享 `lib/util/pack_remap.dart`（`comparePackFiles()`/`totalFileSize()`/`copyPackWithFiles()`，重映射对话框与构建对话框共用）。构建前自动准备环境：`lib/build/toolchain.dart` 检测本机编译器（ICX/clang-cl/MSVC 及版本；ICX 取 `bin/icx.exe`、缺失回退 `icx-cl.exe`；clang-cl 无环境脚本时继承 MSVC 的 `vcvars64.bat`；`selectCompiler()` 按优先级选取，`compilerKindLabel()` 供 UI；`isCompilerUsable()` 校验缓存条目）；`lib/build/build_environment.dart` 的 `prepareBuildEnvironment()`/`preparePackBuildEnvironment()`（缓存有效且匹配优先级 → 直接选择；否则受控 TMP/TEMP 检测 → 选择，新结果经 `onCompilersDetected` 回调由 `MainLayout` 写回设置；再捕获 → 供给 → `assembleBuildEnvironment()`）组装子进程环境——编译器环境经临时 `capture.cmd` 包装（`call "<vcvars/setvars>" >nul 2>&1 && set`，失败退出码传播、临时目录用后清理）捕获，PATH 前置 Ninja → CMake → 编译器附加目录，写 `CNP_CMAKE`/`CNP_NINJA`/`CNP_TOOLS_DIR`/`CNP_C_COMPILER`/`CNP_CXX_COMPILER`/`CNP_COMPILER_KIND`；`lib/build/provisioning.dart` 的 `ToolProvisioner` 本地优先（PATH、`%ProgramFiles%\CMake\bin`，CMake ≥ 3.25），缺失时联网下载官方 zip 解压到 `tools/<名称>/`（版本常量见 `provisioning.dart`；`.source` 标记复用、`tools/.tmp/` 暂存原子替换、3×250ms 重试防句柄锁；内建 HTTP 下载按块回调 `ToolDownloadProgress`（工具名/已下载字节/总字节（`contentLength`，-1 未知）/瞬时速度 bytes/s），各 `ensure*` 调用的 `onDownloadProgress` 由 `prepareBuildEnvironment()`/`preparePackBuildEnvironment()` 透传、加载器注入自定义 `fetch` 时不回调）；`runPackBuild` 新增 `environment` 参数（覆盖层语义，`GIT_TERMINAL_PROMPT`/`SRC_PATH`/`BUILD_OUT`/`PYTHONIOENCODING` 必需键优先）透传 git/python 子进程。构建对话框首阶段为「准备环境」（`prepare` 可注入、显示「编译器：<名称> <版本>」，Key `buildCompilerLabel`）；真实环境冒烟 `test/build/build_environment_real_smoke_test.dart` 由 `CNP_REAL_ENV_SMOKE=1` 门控（检测/捕获/供给/失败传播；联网下载）。
- 「构建」v2 契约与选项：`lib/build/build_script.dart` 扩展——`build.py` 第 2 行起可声明连续 `#` 指令：`# tool: <name> <url> [bin=<子目录>]`（环境工具）、`# option: <name> = <默认值> | <备选值> …`（下拉选项）、`# checkbox: <name> = <勾选值> | <未勾选值>`（复选框，首值=勾选态也是默认，恰 2 值）、`# multiselect: <name> = <值> | …`（多选，值不得含 `;`）、`# source: none`（预构建配方：跳过源码拉取，`SRC_PATH` 转为持久工作缓存目录——工具不清理，脚本下载/解压缓存跨构建复用；`BUILD_OUT` 照常清空）、`# runtime: md|mt`（运行库家族默认，用户未覆盖时生效）与 `# depends: <包名> [<版本范围>]`（自动添加系统依赖，缺省版本按本地包推断）；非法行按注释忽略、同名首次生效、空行终止头部段（`runtime` 为选项保留名，大小写/空白不敏感，以此名声明 `# option`/`# checkbox`/`# multiselect` 时按非法行忽略，防 `CNP_OPTION_RUNTIME` 与 `CNP_RUNTIME_LIBRARY` 取值矛盾）；`BuildScriptOption` 以 `BuildOptionControl`（dropdown/checkbox/multiselect）区分控件类型；`parseBuildScriptHeader()`/`resolveBuildOptions()`/`effectiveBuildOptionValue()`/`multiSelectSelection()`/`normalizedMultiSelectValue()`/`resolveRuntimeLibrary()`/`normalizeRuntimeLibrary()`/`optionEnvName()`/`loadBuildScriptHeader()`（读取包内脚本头部）。声明工具由 `preparePackBuildEnvironment()`（`lib/build/build_environment.dart`）在准备环境阶段下载到 `tools/<name>/` 并注入子进程 PATH；`PYTHONPATH` 前置 `tools/`；选项值经 `CNP_OPTION_<NAME大写>` 下发（多选为声明序 `;` 连接、可空串；`runtime` 为保留键不下发 `CNP_OPTION_*`），运行库经 `CNP_RUNTIME_LIBRARY`（`md`/`mt` 小写）下发，解析顺序 = 用户选择（`buildOptions['runtime']`，值 `MD`/`MT`）> `# runtime:` > `md`（非法逐级回退）。选项 UI（`lib/controls/build_options.dart`）在【文件管理】页工具栏「构建」按钮右侧渲染 `运行库` 标签 + 168 宽 `ComboBox`（`Key('buildRuntimeSelector')`，三项：默认（跟随配方）= 移除保留键 / `MD（动态运行库）` / `MT（静态运行库）`，选择相同值不保存）与「?」帮助按钮（`Key('buildRuntimeHelp')`，`Tooltip.richMessage` 反色说明 MD/MT/d 变体/推荐），下方按声明顺序渲染三型控件分区（`Key('buildOptionsSection')`，有声明且 `onSave` != null 时才渲染、位于版本行上方）：下拉 = `ComboBox`（宽 120 + `comboBoxDensity`，`Key('buildOption_<name>')`）、复选 = 整行可点行（悬停 `surface0` 圆角 4，`Key('buildOption_<name>')`）、多选 = `Wrap` 勾选组（spacing 16 / runSpacing 6 / 组最大宽 420，`Key('buildOptionValue_<name>_<value>')`）；选项行容器 `Key('buildOptionRow_<name>')`，默认展开、可经折叠开关（`Key('buildOptionsToggle')`，chevron + Tooltip）收起/展开，列表 maxHeight 168 纵向滚动（`Key('buildOptionsScroll')`）；变更经全字段拷贝保存为 YAML `buildOptions`（`PackModel` 字段，空省略、读回容错），保存挂起期间全部控件（含运行库下拉）禁用，失败回显旧值；非法保存值经 `effectiveBuildOptionValue()` 显示回退默认（UI 显示与构建下发同口径）。
- 分类辅助模块与 SKILL.md：`assets/build/cnp_build_support.py`（VERSION 6）每次构建前释放到 `tools/cnp_build_support.py`，供 build.py 经 `PYTHONPATH` import——`cmake_configure()`（Ninja + 按 `CNP_RUNTIME_LIBRARY` 注入 MSVC 运行库家族：`md` 缺省/非法回退 → Release `/MD`、Debug `/MDd`；`mt` → `/MT`、`/MTd`；按编译器注入 AVX2/最高优化/IPO 与 `--parallel`；Release 优化 = icx `/O3`、clang-cl `/O2`、msvc `/O2` + 统一追加 `/Ob2 /Oi /Ot /GF /Gy`，clang-cl 用 MSVC 风格旗标——GNU 风格 `-O3` 会被驱动忽略并告警，`/O2`+`/Ot` 实证映射 cc1 `-O3` 级别，clang-cl/icx 已实证接受）/`cmake_build()`/`stage_headers()`/`stage_binaries()`（Release→`release/lib`+`release/bin`、Debug→`debug/lib`+`debug/bin`；`reset=True` 先重置本配置段再 staging——杜绝同名去重改名（`_build-*`）与陈旧文件跨构建累积，默认 `False` 保持旧语义）/`stage_license()`（与 `license_file.dart` 同规则）/`classify_tree()`（预构建产物分类，Debug 判定 = `debug` 目录段「或」文件名 stem 以 `debug_name_suffix` 结尾（缺省 `_debug`，大小写不敏感，供 Release/Debug 同目录发布的归档如 TBB `tbb12_debug.dll` 归入 debug 分层），调用开始打印 `[cnp_build_support] classify: <root> -> <out>` 开始标记，供对话框把阶段切换为「正在分类」）/`summary()`；`_run_process` 以 `subprocess.Popen` 逐行实时打印子进程输出（stderr 合并、UTF-8 容错、失败携带末尾 20 行），真机冒烟 `test/build/build_support_module_test.dart`（含 `stage_binaries` 段重置：预置残留消失、另一段保留、默认兼容与双跑不累积）；库配方 fixture 位于 `test/build/library_recipes/<库>/build.py`，真机验收测试只在临时目录构建、不写用户包目录中的部署副本，fixture 升级后需手工同步部署副本（hash 一致）。设置页「SKILL.md」分区「生成 SKILL.md…」按钮（`Key('settingGenerateSkillButton')`）经 `getSaveLocation` 写出内置模板 `assets/build/SKILL.md`（面向 AI 插件分发；`pickSaveFile`/`loadSkillTemplate` 可注入）。
- 「构建供给链（P3/P4）」：编译器检测覆盖多安装根（`%ONEAPI_ROOT%`、两个 Program Files 根）与新旧布局（`compiler/<版本>/bin`、`windows/bin`，`latest` 兜底），探测与构建子进程统一注入受控 `TMP`/`TEMP`（每次调用在 `tools/.tmp/build/run-*` 下**新建**独立子目录——Q7 根因：ICX 在其临时目录内仅授予 TMP 目录 owner 访问权限，复用他人所有的既有目录会致 `error #10026` 探测/构建失败，新建目录 owner 为当前用户；目录不可写时退回系统临时目录；`createControlledTempDirectory()`/`withControlledTempEnvironment()`/`detectCompilersWithControlledTemp()` 为设置页检测与构建共享的同一机制）；检测全空时 `ensureClangLlvm()` 下载官方 `clang+llvm-*-x86_64-pc-windows-msvc.tar.xz` 到 `tools/clang` 兜底；`ToolProvisioner` 对 CMake/Ninja/LLVM 解析 GitHub 最新版本下载（失败回退内置常量；本地可用优先）、`ensurePython()` 在本地无 Python 时下载 python.org embeddable 到 `tools/python`；`capture.cmd` 以 `&&` 传播环境脚本失败退出码。
- 「构建 v3 界面与系统条目」：进度对话框双栏布局（左阶段/编译器信息 + 右侧流式输出面板，ERROR/WARNING/INFO 字界高亮、自动滚底）；`lib/util/system_entries.dart` 的 `applySystemEntries()` 在重映射/构建保存前同步根级 `pre.bat`/`post.bat` 为「编译设置」系统命令（`"$(MSBuildThisFileDirectory)files\<脚本>" "$(TargetPath)"`，`system: true`）及 `# depends` 声明的系统依赖；系统条目在编译设置/依赖管理两页显示「系统」标记且禁用改删；构建成功记录 `sourceVersion`（成功检出的最新稳定 tag；检出失败或未解析出 tag 时回退 `git describe --tags --abbrev=0`、再回退短哈希），侧栏对含仓库包显示 git 徽标、有新版时替换为「更新」徽标，文件管理页显示「当前版本/最新版本」（`repo_version.dart` 懒查询 + 会话缓存、离线静默）。
- UI 提示统一走 `lib/widgets/floating_toast.dart` 的 `showFloatingToast()`（右上角悬浮、success/info/error 图标配色、默认 3 秒自动关闭 + 手动关闭、同时最多一条、新提示替换旧提示）；「已保存」（3 秒）与配置加载失败警告（error、5 秒、多条合并）均用它。
- 领域模型在 `lib/models/`：`CompilerKind`/`DetectedCompiler`（编译器类型与检测结果，`compiler_model.dart`；`toolchain.dart` 转出）、`PackModel`（含可选 `license`/`iconPath`/`sourcePath`/`sourceVersion`（上次构建记录的仓库版本：git tag、回退短哈希；空省略）、`buildOptions`（构建选项 `{<名称>: <值>}`，空省略；键 = 三个选项指令声明名 + 保留键 `runtime`（运行库 `MD`/`MT`，键不存在 = 跟随配方默认）；供 build.py 选项与运行库下发使用）与 `files`/`dependencies`/`commands`/`macros`/`libDirectories`/`libraries`/`history`/`scripts` 八类列表，`toMap`/`fromMap` 序列化；`scripts` 逐条容错，单条失败丢弃该脚本并写入 warnings）、`DependencyModel`（`name`/`version`/`system`）、`CmdModel`（`command`/`type`/`system`）+`CmdType`（`system: true` 为构建管线自动注册的系统条目，界面禁改删）、`MacroModel`（`value`）、`LibDirModel`（`path`）、`LibraryModel`（`name`）——后三类与命令均含 `buildModel` 三态标签、`FileModel`/`FileType`（序列化只存 `path`/`size`，读回时由 basename 推导 name/type；类型识别覆盖 C++ 系与 pdb/asm/fortran/脚本/LLVM/Python/数据库/exe 等扩展）、`BuildModel`（`all`/`release`/`debug`，`fromName()` 容错解析：缺失→all、非法→FormatException）、`HistoryModel`（`time`/`type`/`message`，`HistoryType`：created/versionChanged/filesChanged/exported）、`SettingsModel`（全局设置：`outputDirectory`/`themeMode`/`darkFlavor`/`accent`/`compilerPriority`（默认 ICX > clang-cl > MSVC；R23 回退迁移：GNU clang 优先级条目改写为 `clang-cl`、含 GNU clang 条目的检测缓存整表作废待重检）/`detectedCompilers`（上次检测缓存：kind/version/可执行文件/环境脚本/附加 PATH，空或坏条目跳过），容错反序列化）、`ScriptProjectModel`（`id`/`name`/`trigger`（pre/post）/`buildModel` + `nodes`/`edges`/viewport；节点参数仅保留 string/bool/num/List<String>，读回时丢弃重复节点 id 与悬空边）。
- `dart:io` 用法集中在 `lib/scanner/file_scan.dart`（`FileScan.scan()` 异步单次遍历包目录，输出相对路径、稳定排序的 `FileModel` 列表；跳过隐藏目录与 `build`/`out`；`FileModel` 含 `size` 字节数）、`lib/util/file_opener.dart`（`openWithDefaultApp()` 用 `Process.run('explorer', ...)` 以系统默认程序打开文件；`revealInExplorer()` 以 `/select,` 两参数在资源管理器中定位文件；`openExternalUrl()` 以 explorer 打开 http/https 链接（默认浏览器）；前二者预检存在性、打开/定位前经 `explorerPath()` 把路径绝对化并将 `/` 统一为 `\`（explorer 对混合分隔符路径静默失败、只打开默认位置）、均不检查退出码——explorer 成功时也恒为 1）、`lib/controls/pack_preview_dialog.dart`（预览时 `File(path).readAsString()` 读取源文件文本）、`lib/build/build_runner.dart`（构建时执行 git / python：`runPackBuild` 默认 `Process.run`，`runPackBuildStreaming`（生产默认）经 `Process.start` 流式逐行回调 `onOutput`——git 与 python 均流式、`\r` 进度按行切分、无效 UTF-8 字节按替换字符容错；`PackProcessRunner`/`PackStreamingProcessRunner` 可注入）、`lib/build/build_cleanup.dart`（构建前 `Directory.list(followLinks: false)` 枚举后递归删除包源目录白名单外条目；根级 `.git` 与构建脚本/图标/钩子脚本/许可证类文件保留，删除失败重试一次并抛 `BuildCleanupException`）、`lib/build/build_environment.dart`（`Process.run('cmd')` 捕获编译器环境（`capture.cmd` 包装、`&&` 传播失败退出码）；受控 `TMP`/`TEMP` 注入）、`lib/build/provisioning.dart`（`HttpClient` 下载工具 zip / python embeddable / clang+llvm tar.xz（XZ+tar 解码）；`archive` 解压；GitHub 最新版解析；`tools/` 原子替换与重试）、`lib/build/repo_version.dart`（`git ls-remote --tags --refs` 远端 tag 查询、`resolveLatestStableTag()` 本地仓库最新稳定 tag 解析——UI「最新版本」与构建 tag 检出共用同一解析/数值比较，tag 前缀形如 `openssl-4.0.2` 可解析、预发布后缀排除）、`lib/packaging/nupkg_exporter.dart`（读取源文件字节并写出 .nupkg）、`lib/packaging/package_icon.dart`（读取图标文件、渲染默认图标资产并输出 PNG 字节）、`lib/build/build_script.dart`（`loadBuildScriptHeader()` 读取包内 `build.py` 头部）与 `lib/pages/setting.dart`（「生成 SKILL.md」写出用户所选文件）。
- **Dart ↔ C++ 无任何桥接**（无 MethodChannel / FFI）；`windows/` 是 Flutter runner 模板，唯一自定义处是窗口标题（`windows/runner/main.cpp`）。要加原生能力需从零自建通道。
- 生成物禁止手改：`windows/flutter/generated_plugin_registrant.*`、`windows/flutter/generated_plugins.cmake`、`windows/flutter/ephemeral/`（均由 Flutter 重新生成）。

## Conventions

- **所有用户可见文案为中文**（硬编码，无 i18n 框架）；新增 UI 文案保持中文。
- UI 使用 `fluent_ui`（Win11 风格）而非 Material；主题色统一走 `lib/util/colors.dart`（`UCColors` / `buildTheme`；深色 flavor 与强调色可配置）。
- 图标：应用自绘 SVG 放 `assets/icons/` 并在 `lib/util/svgs.dart` 注册；目录树图标为第三方子集 `assets/icons/catppuccin/{latte,mocha}/`（catppuccin/vscode-icons v1.26.0，MIT，声明见 `THIRD_PARTY_NOTICES.md`），经 `lib/util/catppuccin_icons.dart` 解析（新增第三方资产须同步声明文件）。
- 测试：`test/` 下为标准 `flutter_test` 测试（冒烟 + 模型 + 扫描 + 配置存储 + 对话框/接线 + 页面 + 列表/工具（含 file_opener 路径规范化）+ 悬浮提示 + 图标映射 + 依赖管理 + 设置页/主题（含检测缓存显示/写回） + 编译设置 + 打包计划/预览/导出/包图标 + CMake 打包 + 缺失依赖警告 + 历史记录 + 关于页/应用信息 + 脚本编辑器（节点注册表/宏白名单/图校验/PS 生成器/语法解析（七图）/prelude 辅助函数（含 crc32 块）/prelude 真实执行冒烟（`ConvertFrom-CnpHex` 往返 + crc32 已知向量 + 生成脚本 while 累加与 `crypto.fileHash` sha256 加固行为锁）/引擎完整性（51 类型）/变量系统（变量节点发射/名称与同名同类型校验/大小写归并初始化）/值条显示 + M2 编辑器：状态控制器/保存队列/画布/连线/节点库/检查器/输出面板/项目对话框/编辑器页 + M3 打包接线：SHA-1/脚本条目与 Target 片段/nupkg 含 .ps1/导出校验（脚本问题/路径查重/exe 警告）/.targets XML 良构性与 CMake 坏脚本护栏 + 编译前/后命令自定义 `Exec` 目标发射（命名/条件/转义/多命令顺序）+ 许可证识别与 `.targets` 部署目标发射 + 构建（build.py 解析/`build_runner`/构建对话框/工具栏接线/打包排除/构建环境：编译器检测与优先级、检测缓存复用（有效直用/失效重检回调/受控 TMP 注入）、vcvars 捕获、CMake-Ninja 供给与环境装配、环境注入（含 `CNP_RUNTIME_LIBRARY` 运行库下发）、真实冒烟（gated）、契约（头部 `# tool`/`# option`/`# checkbox`/`# multiselect`/`# runtime` 解析与选项取值）/选项 UI（三型控件渲染与保存、运行库选择器与帮助 Tooltip、折叠与纵向滚动、保存中禁用、非法值回退）/`buildOptions` 序列化/分类辅助模块（真机分类冒烟；运行库家族 md/mt/非法回退；`stage_binaries` 段重置/默认兼容/双跑不累积；`classify_tree` 的 `_debug` 文件名后缀与缺省参数一致性）/SKILL.md 生成（asset 载入与写出）/构建 v2 延伸（`# source: none`/`# depends` 契约、分类分层与优化参数、Python 供给、TMP/TEMP 注入、ICX 多根检测、clang 兜底供给、最新版解析、系统条目、构建输出面板、git 版本徽标、库配方真机验收（zlib/xsimd/proxy/gtest/sqlite3/mimalloc/OpenVINO/OpenSSL，gated、zlib 纯度断言（release 精确集合、无 `zlibd*`/`zlibsd*`/`_build-*`/`.pdb`，debug 无 `_build-*`）、mimalloc post.bat 编码（ANSI/ASCII、cmd 无参数调用静默）、流式输出（git/python 逐行转发与 `\r` 切分、`-u` 参数、`runPackBuildStreaming` 生产接线、`PYTHONIOENCODING=utf-8` 注入与无效字节容错/真实进程中文输出）、下载进度（按块回调字节与速度、透传、自定义 fetch 不回调）、sourceNone 阶段序列与分类标记、失败保留输出）、构建前清理与预构建缓存复用加固（`build_cleanup` 白名单/根级 `.git` 保留/递归删除/占用报错与 `BuildCleanupException` 包装、git 硬重置序列与失败语义（tag 检出与回退链、绝不盲用 FETCH_HEAD）、source:none 缓存保留、相对 cacheRoot 解析、OpenVINO `.complete` 标记绑定归档驱动））））；widget 测试均用有界 `pump`，禁 `pumpAndSettle`）；新增测试放 `test/`。
- `file_selector` 用于「添加文件夹」的系统原生目录选择（`getDirectoryPath()`，取消返回 null）。
- YAML 配置读写用 Dart 官方 `yaml`/`yaml_edit` 包（封装在 `lib/config/pack_store.dart`）；打包 zip 生成用 `archive` 包（封装在 `lib/packaging/nupkg_exporter.dart` 与 `cmake_exporter.dart`）。
- `README.md` 为正式项目文档（简介/功能特性/打包产物/构建运行/配置文件/版本发布/技术栈/第三方声明）；关键事实以本文件、`ci.yml`、CMake 与代码为准。
